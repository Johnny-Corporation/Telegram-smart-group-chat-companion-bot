[
    {
        "name": "google",
        "description": "This function returns few lines and their links - top results from google search. Assumed to be called together with read_from_link. Use only when necessary",
        "parameters": {
            "type": "object",
            "properties": {
                "question": {
                    "type": "string",
                    "description": "Will be googled"
                }
            },
            "required": [
                "question"
            ]
        }
    },
    {
        "name": "read_from_link",
        "description": "Get content from url",
        "parameters": {
            "type": "object",
            "properties": {
                "link": {
                    "type": "string",
                    "description": "url"
                }
            },
            "required": [
                "link"
            ]
        }
    }
]