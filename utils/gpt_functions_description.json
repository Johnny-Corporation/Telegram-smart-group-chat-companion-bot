[
    {
        "name": "google",
        "description": "Google question",
        "parameters": {
            "type": "object",
            "properties": {
                "question": {
                    "type": "string",
                    "description": "Will be googled"
                }
            },
            "required": [
                "question"
            ]
        }
    },
    {
        "name": "read_from_link",
        "description": "Get content from url",
        "parameters": {
            "type": "object",
            "properties": {
                "link": {
                    "type": "string",
                    "description": "url"
                }
            },
            "required": [
                "link"
            ]
        }
    },
    {
        "name": "generate_image",
        "description": "Generates and sends image(s)",
        "parameters": {
            "type": "object",
            "properties": {
                "prompt": {
                    "type": "string",
                    "description": "Very detailed, only in english"
                },
                "n": {
                    "type": "integer",
                    "description": "Number of images, all images will be sent one by one"
                }
            },
            "required": [
                "prompt"
            ]
        }
    }
]