[
    {
        "name": "google",
        "description": "Use only when there is no way to avoid this, for example when user asks directly or asks some precise data which u dont know",
        "parameters": {
            "type": "object",
            "properties": {
                "question": {
                    "type": "string",
                    "description": "Will be googled"
                }
            },
            "required": [
                "question"
            ]
        }
    },
    {
        "name": "read_from_link",
        "description": "Get content from url",
        "parameters": {
            "type": "object",
            "properties": {
                "link": {
                    "type": "string",
                    "description": "url"
                }
            },
            "required": [
                "link"
            ]
        }
    }
]