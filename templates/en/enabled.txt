Bot is turned on now and reading new messsage to debate!
There are such modes:

Auto mode - it turns on by default. You can know about it and set up parameters /set_up
/enable_dialog - bot will answer on each message
/enable_manual - here you manually add messages to memory and call a bot

Anytime, you can clean memory /clean_memory